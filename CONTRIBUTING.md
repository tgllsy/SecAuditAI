# Contributing

感谢参与 SecAuditAI。提交 Issue 或 Pull Request 前，请先确认：

- 变更范围和复现步骤写清楚；
- 代码已运行 `gofmt`；
- `go test ./...` 和 `go vet ./...` 均通过；
- 不提交 API Key、扫描输出、CodeQL 数据库或本机路径；
- 功能变更同步更新 README 或示例配置。

安全问题请避免在公开 Issue 中直接发布可利用的敏感细节，先联系仓库维护者。
